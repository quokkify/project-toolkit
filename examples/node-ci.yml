name: Node.js CI
on: [pull_request]
permissions:
  contents: read
jobs:
  node:
    uses: quokkify/project-toolkit/.github/workflows/node-ci.yml@v2.3.0
    with:
      package-manager: npm
      lint-command: npm run lint
      test-command: npm test
